import { ItemScrollerProps } from './item-scroller.props.ts';

const ItemScroller = ({
  data,
  surroundingPages = 1,
  pageSize = 50,
  minimumItemsPerPage = 15,
  styles,
  className,
  children,
}: ItemScrollerProps) => {};

export { ItemScroller };
